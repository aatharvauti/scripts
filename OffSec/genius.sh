#!/bin/bash

### **Make Use Of** **Argument Parser**

# `mkdir` `data_time` & use directory to save the output
# IF Ports `80, 443, 8000, 8080` THEN  use ZAPROXY & Nikto Scan, Wfuzzer, curl robots.txt
# ADD to `/etc/hosts`
# ADD Nmap script engine
# `nslookup`
# ADD Hydra Bruteforce `SSH, FTP, Telnet`
# ADD Auto SSH Login and `GET cat <crucial files>`

